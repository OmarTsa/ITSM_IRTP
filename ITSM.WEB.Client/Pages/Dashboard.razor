@page "/dashboard"
@using ITSM.Entidades
@* ELIMINAMOS TicketNegocio *@
@inject TicketServicio _ticketServicio

<MudText Typo="Typo.h4">Dashboard</MudText>
<MudText>Bienvenido al sistema ITSM</MudText>

@* Aquí irían tus gráficos. Por ahora lo dejamos simple para que compile *@

@code {
    protected override async Task OnInitializedAsync()
    {
        // Lógica futura con _ticketServicio
        await Task.CompletedTask;
    }
}